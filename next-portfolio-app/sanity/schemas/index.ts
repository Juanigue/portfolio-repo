//All the chemas are definied on un single page like this index.ts so its more organized then for be used.

import project from './project-schema';

const schemas = [project];

export default schemas;